type: object
required:
  - action
properties:
  action:
    type: string
    enum: [OK, SYNC, CLEAR_BOOKS, RESET, UPGRADE]
  extraMessage:
    description: For example, the url of the new apk to upgrade
    type: string